/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["plus.unsplash.com", "images.unsplash.com"], // Agrega el dominio aquí
      },
};

export default nextConfig;
